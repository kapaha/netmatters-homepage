{
    "name": "netmatters-homepage-clone",
    "private": true,
    "version": "0.0.0",
    "description": "A clone of the netmatters homepage",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "predeploy": "npm run build",
        "deploy": "gh-pages -d dist"
    },
    "devDependencies": {
        "gh-pages": "^6.0.0",
        "sass": "^1.69.3",
        "vite": "^4.4.5"
    },
    "dependencies": {
        "normalize.css": "^8.0.1"
    }
}
